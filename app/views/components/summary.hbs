{{#resume.basics}}
  {{#if summary}}
    <div class="container summary-container">
      {{> title value=(t "summary.title")}}
      <p class="summary">
        {{summary}}
      </p>
    </div>
  {{/if}}
{{/resume.basics}}
